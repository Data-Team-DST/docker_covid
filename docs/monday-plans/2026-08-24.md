# Plan semaine 2026-08-24

> Note méthodologique : c'est le premier `/monday` de ce repo. Le rapport `/friday` source
> (`docs/friday-audits/2026-08-24_11h11.md`) a été généré **ce matin même** (11h11), pas le
> vendredi précédent — le rituel `/friday` vient d'être utilisé pour la première fois juste
> après le commit `aaf4d18` (setup Claude Code). Il n'y a donc eu **aucune activité git depuis
> ce rapport** : le re-check adaptatif de l'Étape 2 est sans objet, le rapport friday fait
> foi intégralement. Les "3 commits depuis last friday" (git log --since="last friday") sont
> les commits qui ont précédé et amené ce rapport, pas de l'activité postérieure.

## Rappel findings vendredi

| # | Action | Service | P | Estimation | Statut |
|---|--------|---------|---|-----------|--------|
| 1 | Nettoyer wildcards dangereux `.claude/settings.local.json` | .claude | P1 | 20 min | ✅ Fait |
| 2 | Retirer `--reload` du CMD `infrastructure/docker/backend/Dockerfile` | backend | P1 | 10 min | ✅ Fait (commit `04255fc`) |
| 3 | Fail-fast sur `API_KEY` vide quand `API_ENV=production` | backend | **P1** | 30 min | ✅ Fait (commit `34c41a0`) |
| 4 | Ne plus renvoyer `str(e)` brut sur les 500 de `/api/v1/predict` | backend | P2 | 20 min | ✅ Fait (commit `aab430e`) |
| 5 | Test CI qui appelle réellement `/api/v1/predict` (pas juste `/health`) | CI/CD | P2 | 1h | ⬜ À faire |
| 6 | Extraire `lifespan` et `_log_requests` de `backend/app/main.py` (89→~30 lignes) | backend | P2 | 1h | ✅ Fait (commit `600af93`) |
| 7 | Pinner `checkout`/`setup-python`/`upload-artifact`/`download-artifact` sur SHA (`cicd.yml`) | CI/CD | P2 | 1h | ✅ Fait (commit `47dc264`) |
| 8 | `USER` non-root (2 Dockerfiles) + `securityContext`/`runAsNonRoot` (manifests K8s) | backend, data-service | P2 | 1h | ✅ Fait (commit `37f6c59`) |
| 9 | Politique pour les 75 disables lint `frontend/page/` | frontend | P2 | 30 min (décision) | ✅ Fait (commit `ccba75d`) |
| 10 | Retirer check `comment-analyzer` cassé de `friday/SKILL.md` | .claude | P2 | — | ✅ Fait (commit `04255fc`) |
| 19 | Clarifier contradiction règle #7 CLAUDE.md vs `.gitignore` réel (`.claude/rules|skills|agents` publics) | .claude | P2 | 15 min (décision) | ✅ Fait (commit `e16105b`) |
| 11 | Ruff auto-fix + B017/B905 manuel | tous | P3 | 20 min | ✅ Fait (commit `0a66899`) |
| 12 | `--only-binary=":all:"` sur `pip install ruff/pylint` | CI/CD | P3 | 10 min | ✅ Fait (commit `9c6fd9e`) |
| 13 | `permissions: {}` au niveau workflow | CI/CD | P3 | 5 min | ✅ Fait (commit `9c6fd9e`) |
| 14 | Commenter l'intention du `pylint ... \|\| true` | CI/CD | P3 | 5 min | ✅ Fait (commit `9c6fd9e`) |
| 15 | Installer `radon` (dev-requirements) | tous | P3 | 15 min | ✅ Fait (commit `e2ca6bd`) |
| 16 | Épingler `image: :latest` sur un tag immuable (K8s backend) | K8s | P3 | 15 min | ✅ Fait (commit `e19b002`) |
| 17 | Ne plus committer la ressource K8s `Secret` en clair | K8s | P3 | 20 min | ✅ Fait (commit `e19b002`) |
| 18 | Ajouter un grep email/nom-propre aux checks sécurité `/friday` | .claude | P3 | 10 min | ✅ Fait (commit `767a6fd`) |
| — | **Hors plan initial** — `pywin32==312` cassait le build Docker/CI data-service (lock file résolu sous Windows) | data-service | **P1** | — | ✅ Fait (commit `de6afbc`) |

*(Tableau complet des 19 actions dans le rapport friday — ci-dessus : uniquement P1/P2 + un
aperçu groupé du P3.)*

## Ce qui a bougé depuis vendredi

Rien en termes de commits (0 depuis la génération du rapport). Mais **le rapport lui-même a
généré une quantité importante de changements non commités**, actuellement dans l'arbre de
travail (voir Blockers). Ces changements sont déjà "faits" au sens du rapport friday
(§"Actions appliquées cette session"), il manque juste le commit.

## Résultats lint post-weekend

✅ Mis à jour en cours de journée : `ruff check .` repo-wide propre (0 erreur, était 23),
backend à 95% de coverage (était 77.5%, seuil 80%) — voir Priorité 1 et action #11.

## Contexte sprint

Backlog (`dashboard/backlog.yaml`) : **Sprint S3** "CI/CD complet & API production-ready" —
`status: in_progress`, deadline `2026-04-24` **dépassée de 4 mois** (cohérent avec l'alerte de
`CLAUDE.md` § Calendrier — à reclarifier avec Steven si ce n'est pas déjà fait).

Stories S3 non terminées :
- **US-15** (load test P95) : bloquée, aucun modèle entraîné dans `data/models/` — dépend de
  `dvc repro`/US-08. Pas d'action possible tant que le pipeline n'a pas tourné en entier.
- **US-16** (data augmentation) : `done: false` dans le backlog, mais le rapport friday +
  la mémoire projet indiquent qu'un stage `augment` a été ajouté au pipeline DVC
  (`dvc.yaml`/`dvc.lock`, commit `35ee130`). **À vérifier avec Steven si US-16 doit passer
  `done: true`** — pas fait automatiquement ici (backlog = décision humaine).
- **US-23** (Kubernetes) : `done: false`, manifests déjà écrits, jamais déployés sur un
  cluster réel. Marqué `MUST` mais aussi noté "optionnel" ailleurs — ambiguïté déjà signalée
  dans le backlog lui-même, à trancher si la soutenance approche.

Croisement avec les actions friday : les actions #3 (fail-fast API_KEY), #4 (leak `str(e)`),
#8 (USER non-root/securityContext) sont exactement les blockers/high-value fixes remontés par
`production-audit` (score 61/100) dans le rapport — **aucune US dédiée dans le backlog**
pour ce durcissement sécurité production. Dette hors-sprint, à faire remonter en US si
Steven veut la tracer formellement (sinon elle reste "hygiène continue").

## Blockers à anticiper

- ✅ **Résolu** — Arbre de travail non commité : commité dans `04255fc` (rituel friday, 55
  fichiers) puis `34c41a0` (fail-fast API_KEY). `git status` propre après les deux commits.
  **Non poussé** — `git push` reste soumis à validation explicite (CLAUDE.md § Validation
  humaine obligatoire), pas fait automatiquement ici.
- ✅ **Résolu** — Backend sous le nouveau seuil de coverage : 77.5% → **95%** après ajout de
  `backend/tests/unit/test_predict.py` (chemin succès + gestion d'erreur 500 de `/predict`,
  auparavant non testés) et `test_loader.py` (`ModelLoader.load()`/`predict()`, fichier
  manquant/modèle chargé/échec de chargement, auparavant à 39%). 67/67 tests backend verts,
  lint propre. `predict.py`, `loader.py`, `config.py` à 100%. Reste sous 100% uniquement
  `main.py` (80%, lignes du `lifespan` non exercées par `TestClient` sans context manager —
  hors périmètre de cette tâche).
- **Dépendances inter-services** : aucune détectée cette semaine (pas de nouvel import
  cross-service signalé par friday).
- **CI en attente** : aucun run en cours connu (dernier push = `aaf4d18`, statut GitHub non
  vérifié via `gh` — signalé comme "evidence missing" dans le rapport friday).

## Plan de la semaine

### Priorité 1 — À faire aujourd'hui

1. ✅ **Fait** — Commit du travail du rituel friday de ce matin (`04255fc`, 55 fichiers).
2. ✅ **Fait** — Action #3, fail-fast `API_KEY` vide en prod (`34c41a0`) : `model_validator`
   Pydantic sur `Settings`, refuse de démarrer si `api_env="production"` et `api_key=""`.
   3 nouveaux tests (`backend/tests/unit/test_config.py`), 60/60 tests backend verts,
   `config.py` à 100% de couverture. Note : `verify_api_key()` (`app/api/security.py`)
   n'a pas été touché — le fail-open au niveau requête reste la même logique, mais devient
   inatteignable en prod puisque le process ne démarre plus sans clé.
3. ✅ **Fait** — Coverage backend remontée à 95% (voir Blockers) via `test_predict.py` +
   `test_loader.py`.

### Priorité 2 — restant

- Action #5 : test CI réel sur `/api/v1/predict` (1h) — comble le 2ᵉ blocker production-audit
  (chemin critique jamais testé end-to-end). **Seul item P1/P2 encore ouvert.**
- Clarifier avec Steven le statut US-16 (probablement à passer `done: true`) et la deadline
  Sprint S3 dépassée (US-23 K8s : bloquant soutenance ou non ?).
- La moitié des 71 disables lint `frontend/` non couverte par l'exemption Streamlit
  (`broad-exception-caught`, `invalid-name`, `too-many-locals`, `line-too-long`,
  `missing-function-docstring`, `unused-import`, `I001`) reste à traiter séparément si
  souhaité — décision #9 ne portait que sur le pattern d'import.

### Priorité 3 — toutes traitées

Items #11 à #18 tous faits (voir tableau ci-dessus). Rien en attente côté P3.

## Tâche de démarrage recommandée

Tout le tableau "Actions prioritisées" du rapport friday est traité, à l'exception de
l'action #5 (test CI réel sur `/api/v1/predict`).

**→ Prochaine étape : Action #5** — ajouter un test CI qui appelle réellement
`/api/v1/predict` avec une image dans le smoke-test de déploiement (`cicd.yml` job
`deploy`), qui aujourd'hui ne vérifie que `/health` et `/docs`. Nécessite un modèle
entraîné disponible pendant le smoke-test (ou un test qui accepte un 503 modèle-non-chargé
comme réponse valide, à discuter) — voir aussi US-15 (load test), bloquée pour la même
raison (aucun modèle dans `data/models/`).

## Découverte hors plan initial

En testant le changement non-root (action #8), le build Docker de `data-service` s'est
révélé cassé : `pywin32==312` (paquet Windows uniquement) était figé en dur dans
`requirements.txt`, généré par erreur sur cette machine Windows au lieu d'un environnement
Linux. Ce lock file cassait tout build/install sur `ubuntu-latest` (CI) — donc
potentiellement déjà rouge sur GitHub avant ce correctif. Régénéré et validé dans un
conteneur Linux Python 3.11 (commit `de6afbc`).
