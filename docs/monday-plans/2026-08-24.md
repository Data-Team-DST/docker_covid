# Plan semaine 2026-08-24

> Note méthodologique : c'est le premier `/monday` de ce repo. Le rapport `/friday` source
> (`docs/friday-audits/2026-08-24_11h11.md`) a été généré **ce matin même** (11h11), pas le
> vendredi précédent — le rituel `/friday` vient d'être utilisé pour la première fois juste
> après le commit `aaf4d18` (setup Claude Code). Il n'y a donc eu **aucune activité git depuis
> ce rapport** : le re-check adaptatif de l'Étape 2 est sans objet, le rapport friday fait
> foi intégralement. Les "3 commits depuis last friday" (git log --since="last friday") sont
> les commits qui ont précédé et amené ce rapport, pas de l'activité postérieure.

## Rappel findings vendredi

| # | Action | Service | P | Estimation | Statut |
|---|--------|---------|---|-----------|--------|
| 1 | Nettoyer wildcards dangereux `.claude/settings.local.json` | .claude | P1 | 20 min | ✅ Fait |
| 2 | Retirer `--reload` du CMD `infrastructure/docker/backend/Dockerfile` | backend | P1 | 10 min | ✅ Fait (non commité) |
| 3 | Fail-fast sur `API_KEY` vide quand `API_ENV=production` | backend | **P1** | 30 min | ⬜ À faire |
| 4 | Ne plus renvoyer `str(e)` brut sur les 500 de `/api/v1/predict` | backend | P2 | 20 min | ⬜ À faire |
| 5 | Test CI qui appelle réellement `/api/v1/predict` (pas juste `/health`) | CI/CD | P2 | 1h | ⬜ À faire |
| 6 | Extraire `lifespan` et `_log_requests` de `backend/app/main.py` (89→~30 lignes) | backend | P2 | 1h | ⬜ À faire |
| 7 | Pinner `checkout`/`setup-python`/`upload-artifact`/`download-artifact` sur SHA (`cicd.yml`) | CI/CD | P2 | 1h | ⬜ À faire |
| 8 | `USER` non-root (2 Dockerfiles) + `securityContext`/`runAsNonRoot` (manifests K8s) | backend, data-service | P2 | 1h | ⬜ À faire |
| 9 | Politique pour les 75 disables lint `frontend/page/` | frontend | P2 | 30 min (décision) | ⬜ À faire |
| 10 | Retirer check `comment-analyzer` cassé de `friday/SKILL.md` | .claude | P2 | — | ✅ Fait (non commité) |
| 19 | Clarifier contradiction règle #7 CLAUDE.md vs `.gitignore` réel (`.claude/rules|skills|agents` publics) | .claude | P2 | 15 min (décision) | ⬜ À faire |
| 11-18 | Ruff auto-fix, SHA pinning secondaire, `permissions: {}` workflow, radon, K8s image tag, K8s Secret, grep email/nom | tous | P3 | ~1h30 cumulé | ⬜ Backlog |

*(Tableau complet des 19 actions dans le rapport friday — ci-dessus : uniquement P1/P2 + un
aperçu groupé du P3.)*

## Ce qui a bougé depuis vendredi

Rien en termes de commits (0 depuis la génération du rapport). Mais **le rapport lui-même a
généré une quantité importante de changements non commités**, actuellement dans l'arbre de
travail (voir Blockers). Ces changements sont déjà "faits" au sens du rapport friday
(§"Actions appliquées cette session"), il manque juste le commit.

## Résultats lint post-weekend

Inchangé — voir rapport friday (23 erreurs ruff, 21 auto-fixables ; backend 77.5% coverage
< nouveau seuil 80% depuis le changement de seuil appliqué ce matin).

## Contexte sprint

Backlog (`dashboard/backlog.yaml`) : **Sprint S3** "CI/CD complet & API production-ready" —
`status: in_progress`, deadline `2026-04-24` **dépassée de 4 mois** (cohérent avec l'alerte de
`CLAUDE.md` § Calendrier — à reclarifier avec Steven si ce n'est pas déjà fait).

Stories S3 non terminées :
- **US-15** (load test P95) : bloquée, aucun modèle entraîné dans `data/models/` — dépend de
  `dvc repro`/US-08. Pas d'action possible tant que le pipeline n'a pas tourné en entier.
- **US-16** (data augmentation) : `done: false` dans le backlog, mais le rapport friday +
  la mémoire projet indiquent qu'un stage `augment` a été ajouté au pipeline DVC
  (`dvc.yaml`/`dvc.lock`, commit `35ee130`). **À vérifier avec Steven si US-16 doit passer
  `done: true`** — pas fait automatiquement ici (backlog = décision humaine).
- **US-23** (Kubernetes) : `done: false`, manifests déjà écrits, jamais déployés sur un
  cluster réel. Marqué `MUST` mais aussi noté "optionnel" ailleurs — ambiguïté déjà signalée
  dans le backlog lui-même, à trancher si la soutenance approche.

Croisement avec les actions friday : les actions #3 (fail-fast API_KEY), #4 (leak `str(e)`),
#8 (USER non-root/securityContext) sont exactement les blockers/high-value fixes remontés par
`production-audit` (score 61/100) dans le rapport — **aucune US dédiée dans le backlog**
pour ce durcissement sécurité production. Dette hors-sprint, à faire remonter en US si
Steven veut la tracer formellement (sinon elle reste "hygiène continue").

## Blockers à anticiper

- **Arbre de travail non commité important** : 16 fichiers modifiés + 6 nouveaux dossiers
  skills (`eval-harness`, `production-audit`, `rules-distill`, `security-scan`, `skill-comply`,
  `skill-stocktake`) + `.reports/`, `docs/CODEMAPS/`, `docs/friday-audits/` non trackés.
  Ce sont les changements produits par le rituel friday de ce matin (seuil de couverture
  unifié à 80%, retrait `--reload`, wildcards nettoyés, 15 corrections de contenu `.claude/`,
  codemaps générés). **Rien n'est perdu**, mais tant que ce n'est pas commité, `git status`
  n'est pas propre — contredit le critère "Done" #4 de `CLAUDE.md`. Pas de stash oublié
  (`git stash list` vide).
- **Backend sous le nouveau seuil de coverage** : 77.5% mesuré < 80% requis depuis ce matin —
  accepté explicitement par Steven dans le rapport friday, mais reste un état "cassé" tant
  que des tests backend ne sont pas ajoutés. Le CI (`cicd.yml`) va probablement échouer sur
  `test_be` au prochain push tant que ce n'est pas résorbé.
- **Dépendances inter-services** : aucune détectée cette semaine (pas de nouvel import
  cross-service signalé par friday).
- **CI en attente** : aucun run en cours connu (dernier push = `aaf4d18`, statut GitHub non
  vérifié via `gh` — signalé comme "evidence missing" dans le rapport friday).

## Plan de la semaine

### Priorité 1 — À faire aujourd'hui

1. **Commiter le travail du rituel friday de ce matin** (16 fichiers + skills + rapports),
   après relecture du diff — condition préalable à tout le reste, `git status` doit redevenir
   propre avant d'attaquer une nouvelle tâche (`CLAUDE.md` critère #4).
2. **Action #3 — Fail-fast `API_KEY` vide en prod** (`backend/app/config.py` +
   `app/api/security.py`) — 30 min, seul vrai blocker sécurité restant du score
   production-audit (61/100).
3. **Décider du sort de la coverage backend à 77.5%** : soit ajouter des tests rapidement
   pour repasser ≥80% avant le prochain push CI, soit accepter temporairement un CI rouge sur
   `test_be` en connaissance de cause — à trancher avec Steven, pas une décision technique
   seule.

### Priorité 2 — À faire cette semaine

- Action #4 : ne plus renvoyer `str(e)` brut sur les 500 de `/api/v1/predict` (20 min).
- Action #5 : test CI réel sur `/api/v1/predict` (1h) — comble le 2ᵉ blocker production-audit
  (chemin critique jamais testé end-to-end).
- Action #6 : extraire `lifespan`/`_log_requests` de `backend/app/main.py` (1h).
- Action #7 : pinner les 4 actions GitHub restantes sur SHA (1h).
- Action #8 : `USER` non-root + `securityContext` K8s (1h).
- Action #9 : politique disables lint `frontend/page/` (30 min, décision).
- Action #19 : clarifier avec Steven la contradiction règle #7 CLAUDE.md / `.gitignore` réel.
- Clarifier avec Steven le statut US-16 (probablement à passer `done: true`) et la deadline
  Sprint S3 dépassée (US-23 K8s : bloquant soutenance ou non ?).

### Priorité 3 — Backlog / si le temps le permet

- `ruff check --fix` (21 fixes auto) + corriger `B017`/`B905` à la main (20 min).
- `--only-binary=":all:"` sur les `pip install ruff/pylint` de `cicd.yml` (10 min).
- `permissions: {}` au niveau workflow dans `cicd.yml` (5 min).
- Commenter l'intention du `pylint ... || true` (5 min).
- Installer `radon` (dev-requirements) pour activer le check de complexité au prochain friday.
- Épingler `image: :latest` sur un tag `sha-` immuable (K8s backend, 15 min).
- Ne plus committer la ressource K8s `Secret` en clair (20 min).
- Ajouter un grep email/nom-propre aux checks sécurité du prochain `/friday` (10 min).

## Tâche de démarrage recommandée

**→ Commiter le travail du rituel friday de ce matin (`git add` ciblé + relecture diff,
~15 min), puis enchaîner sur l'action #3 (fail-fast `API_KEY` vide en prod, `backend/`,
30 min).**

Rien d'autre n'est raisonnable à démarrer tant que le `git status` n'est pas propre — c'est
le seul vrai blocker de la matinée, et l'action #3 est le blocker sécurité restant le plus
rapide du score production-audit (61/100).
