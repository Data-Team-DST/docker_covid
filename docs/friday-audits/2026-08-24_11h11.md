# Audit vendredi 2026-08-24 11h11

> Note méthodologique : ce rituel s'est exécuté sans `make` (absent de cette machine). Il a
> démarré sans les 7 skills + 4 commands que `SKILL.md` référence en interne — vérifications
> équivalentes faites à la main (voir Findings ci-dessous) — puis l'utilisateur a fourni le
> paquet manquant en cours de session ; les phases initialement bloquées ont été reprises,
> voir section "Addendum" plus bas pour leurs résultats.

## Métriques semaine

| Métrique | Valeur | Δ vs semaine précédente |
|----------|--------|------------------------|
| Lint (ruff, tout le repo) | 23 erreurs (21 auto-fixables) | — (première mesure) |
| Coverage backend | 77.5% (seuil 40%, 57 tests) | — |
| Coverage data-service | 83.3% (seuil 30%, 24 tests) | — |
| Commits semaine | 17 | — |
| Fichiers touchés (union des commits) | ~150 (dont suppression massive de code mort) | — |

*Pas de `make quality` disponible sur cette machine (pas de `make`) — mesures obtenues via
`ruff check .` et `pytest` direct sur l'interpréteur global (les venvs isolés `backend/.venv`
et `data-service/.venv` n'existent pas ici).*

## Activité semaine

Semaine très chargée (17 commits) centrée sur trois axes :

- **Nettoyage de code mort** : suppression de `backend/src/explorationdata/`, `backend/src/features/Pipelines/`,
  `backend/src/utils/`, `cli.py`, `image_processor.py`, `src/requirements.txt` — gros ménage,
  cohérent avec la philosophie "mesurer avant nettoyer" déjà appliquée.
- **Pipeline DVC** : nouveau stage `augment` (US-16), fix fuite train/test, fix OOM preprocess/train,
  CLAHE paramétrable via `params.yaml`, intégration du modèle/protocole de `train.ipynb` dans le
  pipeline DVC (`dvc.yaml`/`dvc.lock` mis à jour).
- **Setup Claude Code** (dernier commit `aaf4d18`) : ajout de `CLAUDE.md`, `.claude/rules/`,
  `.claude/skills/`, `.claude/agents/` + un audit sécurité — c'est ce commit qui rend ce rituel
  `/friday` utilisable pour la première fois sur ce repo.

Deux commits avec message non descriptif (`ok`, `ok`) — mineur, à surveiller si ça devient un pattern.

## Findings

### Sécurité

- **RAS sur les fondamentaux** : pas de `verify=False`, pas de secret hardcodé détecté, `.env`
  correctement gitignoré, aucun `.env*` tracké par git.
- **CORS `allow_origins=["*"]`** dans `backend/app/main.py:60` et
  `data-service/src/data_service/main.py:22` — sans `allow_credentials=True`, donc pas la
  vulnérabilité RFC 6454 documentée dans `sonarqube.md`. Reste un CORS grand ouvert (P3, à
  restreindre avant la Phase 3 Kubernetes si ces services deviennent accessibles publiquement).
- **Notebooks `test.ipynb` / `train.ipynb` / `train_segmentation.ipynb`** (tracés par git,
  contiennent des sorties de cellules — images, stream, un traceback `KeyboardInterrupt`) :
  aucun chemin local, identifiant ou secret détecté dans les sorties. RAS, mais ce sont des
  fichiers volumineux (270-430 Ko chacun, sorties base64 incluses) à surveiller.
- **Config agentic Claude Code** : pas de `.claude/hooks/`, pas de `.mcp.json`, pas de secret
  dans les fichiers de mémoire, pas d'unicode caché dans `.claude/rules|skills|agents`,
  `.claude/settings.local.json` non tracké par git. RAS.
- **Écart entre `CLAUDE.md` règle #7 et l'état réel du `.gitignore`** : la règle dit
  "`.claude/` doit rester gitignoré en permanence", mais `.gitignore` exempte explicitement
  `.claude/rules/`, `.claude/skills/`, `.claude/agents/` et `.claude/CLAUDE_AUDIT.md`, et ces
  dossiers sont déjà commités et poussés (commit `aaf4d18`, repo public). Contenu relu : rien
  de sensible dedans. À clarifier avec l'utilisateur si c'est le comportement voulu (transparence
  volontaire sur la config Claude Code) ou si la règle #7 doit être reformulée pour ne plus
  contredire la pratique. **P2 — ambiguïté à trancher, pas une fuite avérée.**
- **`/friday` référence des skills inexistants** : `/repo-scan`, `/production-audit`,
  `/refactor-clean`, `/security-scan`, `/update-codemaps`, `/update-docs`, `/skill-comply`,
  `/skill-stocktake`, `/rules-distill`, `/eval-harness`, agent `comment-analyzer` — aucun n'est
  présent dans `.claude/skills/` ni `.claude/agents/` de ce repo. Le rituel hebdomadaire ne peut
  pas s'exécuter tel que documenté. **P2 — soit importer ces skills, soit réécrire `SKILL.md`
  pour ne référencer que l'outillage réellement présent.**

### Architecture & production-readiness

- **CI/CD (`​.github/workflows/cicd.yml`, touché cette semaine)** — audité contre
  `github-actions-security.md` :
  - ✅ Permissions déclarées au niveau job (pas de bloc global) — conforme.
  - ✅ `docker/*`, `SonarSource/sonarcloud-github-action` correctement épinglés sur SHA complet
    avec commentaire de version.
  - ✅ Installs `-r requirements.txt` utilisent `--require-hashes`, fichiers générés via
    `pip-compile --generate-hashes` (vérifié : 910 à 1892 hashs par fichier selon le service).
  - ❌ **`actions/checkout@v4`, `actions/setup-python@v5`, `actions/upload-artifact@v4`,
    `actions/download-artifact@v4` restent sur des tags mutables**, alors que la règle interne
    exige un SHA complet pour "toute action tierce" — et c'est précisément l'exemple `❌` cité
    dans `github-actions-security.md`. Incohérent avec le soin apporté aux actions `docker/*`
    dans le même fichier. ~9 occurrences de `checkout@v4` dans le workflow. **P2, ~1h** (résoudre
    les 4 SHA, tester le run).
  - ❌ **Pas de bloc `permissions: {}` au niveau `workflow`** — seuls des blocs par job
    existent, ce qui couvre le risque en pratique mais s'écarte de la checklist "deny-all
    explicite" de la règle. **P3, 5 min.**
  - ❌ **`pip install -q ruff==0.8.6 pylint==3.3.9` (jobs `lint_be`, `lint_ds`) sans
    `--only-binary=":all:"`** — contredit S8541 (risque `setup.py` arbitraire si pip retombe sur
    un sdist). Faible risque réel (ruff/pylint sont distribués en wheels), mais rule violée.
    **P3, 10 min.**
  - ⚠️ **`pylint --fail-under=7 ... || true`** (lint_be, lint_ds) : le `|| true` rend ce step
    toujours vert quel que soit le score pylint — la gate pylint est décorative, seul ruff bloque
    réellement le pipeline. Si c'est voulu (ruff = gate, pylint = informatif), très bien, mais ça
    mérite d'être documenté en commentaire dans le workflow pour que ça ne soit pas lu comme un
    oubli. **P3, 5 min (juste un commentaire).**
- **`backend/app/main.py` : 89 lignes pour un `main.py`** (limite 50, marge 10% = 55) — contient
  le `lifespan`, le middleware de logging `_log_requests`, la config CORS et une route `/`, en
  plus du câblage des routers. Dépasse largement la marge justifiée par `coding-style.md`.
  Extraction simple : `lifespan` → `app/lifespan.py`, `_log_requests` → `app/middleware.py`.
  **P2, ~1h (mécanique, faible risque, à re-tester après coup).**
- **DVC** : remote `minio` configuré (`s3://dvcstore`, `http://localhost:9000`), aucun fichier
  `.dvc`/`dvc.yaml`/`dvc.lock` en attente de commit (`git status` propre sur ces fichiers).
  `dvc status` non exécutable depuis cette machine (module `dvc` absent hors venvs de service) —
  **non vérifié**, à recroiser au prochain `/friday` si un venv avec DVC est disponible.

### Dette technique

- **Ruff : 23 erreurs**, 21 auto-fixables (`I001` imports non triés ×8, `F401` imports inutilisés
  ×4, `UP017`/`W291`/`F541`/`UP045`/`W293`), 2 non auto-fixables (`B017` assert exception générique
  dans `backend/tests/unit/test_preprocessing.py:98`, `B905` zip sans `strict=` explicite).
  **P3, 15 min avec `ruff check --fix` + 1 correction manuelle du `B017`.**
- **75 disables de lint (`pylint: disable`, `noqa:`, `type: ignore`) sur 30 fichiers**, concentrés
  presque exclusivement dans `frontend/page/` (72/75) + 3 dans `scripts/*.py` et
  `backend/app/logging_config.py`. La grande majorité sont des `# noqa: E402` liés au pattern
  Streamlit (imports après `sys.path.insert`) — struturellement nécessaires, mais sans la
  justification en langage naturel exigée par `sonarqube.md`. Vu le volume (75), retro-justifier
  ligne par ligne n'a pas de sens : la vraie action est un choix de politique (commentaire-type
  unique en tête de fichier expliquant le pattern Streamlit, ou exemption documentée de la règle
  pour `frontend/page/`). **P2 — décision à prendre, ~2h si on choisit la retro-justification
  fichier par fichier.**
- **`radon` non disponible** sur cette machine — check de complexité cyclomatique dédié (Phase 4)
  non exécuté cette semaine. À refaire au prochain `/friday` si `radon` est installé dans un venv
  service, ou l'ajouter à `requirements-dev`.
- **Fichiers hors gabarit** : seul `backend/app/main.py` détecté hors limite (voir ci-dessus,
  section Architecture). Aucun `_router.py`/`_service.py`/`_utils.py` en dépassement.

### DVC & données

- `data/raw.dvc` : 806 MB, 42 335 fichiers, hash inchangé cette semaine (aucune modif de
  `*.dvc` dans les commits de la semaine).
- Nouveau stage `augment` (US-16, commit `35ee130`) intégré au pipeline `dvc.yaml`/`dvc.lock` —
  correspond à l'US en attente listée dans `CLAUDE.md` § Calendrier. À vérifier lundi si l'US-16
  peut être marquée traitée.
- Remote MinIO configuré (`s3://dvcstore` / `localhost:9000`) — remplace l'ancienne situation
  "pas de remote" notée en mémoire de session ; mémoire projet à mettre à jour.

## Actions prioritisées

| # | Action | Service | P | Estimation | À planifier |
|---|--------|---------|---|-----------|-------------|
| 1 | ✅ **Fait** — Nettoyer les wildcards dangereux dans `.claude/settings.local.json` | .claude | **P1** | 20 min | Fait |
| 2 | ✅ **Fait** — Retirer `--reload` du CMD de `infrastructure/docker/backend/Dockerfile` | backend | **P1** | 10 min | Fait |
| 3 | Fail-fast sur `API_KEY` vide quand `API_ENV=production` (actuellement fail-open silencieux) | backend | **P1** | 30 min | Semaine N+1 |
| 4 | Ne plus renvoyer `str(e)` brut sur les 500 de `/api/v1/predict` (fuite de détail interne) | backend | P2 | 20 min | Semaine N+1 |
| 5 | Ajouter un test CI qui appelle réellement `/api/v1/predict` (pas juste `/health`/`/docs`) | CI/CD | P2 | 1h | Semaine N+1 |
| 6 | Extraire `lifespan` et `_log_requests` de `backend/app/main.py` (89→~30 lignes) | backend | P2 | 1h | Semaine N+1 |
| 7 | Pinner `actions/checkout`, `setup-python`, `upload-artifact`, `download-artifact` sur SHA dans `cicd.yml` | CI/CD | P2 | 1h | Semaine N+1 |
| 8 | `USER` non-root dans les 2 Dockerfiles + `securityContext`/`runAsNonRoot` dans les manifests K8s | backend, data-service | P2 | 1h | Semaine N+1 |
| 9 | Décider une politique pour les 75 disables `frontend/page/` (commentaire-type vs retro-justification) | frontend | P2 | 30 min (décision) | Semaine N+1 |
| 10 | ✅ **Fait** — Check `comment-analyzer` désactivé dans `friday/SKILL.md` (agent créé from scratch, hors périmètre — juste la référence cassée retirée) | .claude | P2 | — | Fait |
| 11 | `ruff check --fix` (21 fixes auto) + corriger `B017`/`B905` à la main | tous | P3 | 20 min | Backlog |
| 12 | Ajouter `--only-binary=":all:"` sur les `pip install ruff/pylint` de `cicd.yml` | CI/CD | P3 | 10 min | Backlog |
| 13 | Ajouter `permissions: {}` au niveau workflow dans `cicd.yml` | CI/CD | P3 | 5 min | Backlog |
| 14 | Documenter en commentaire l'intention du `pylint ... \|\| true` (gate informative, pas bloquante) | CI/CD | P3 | 5 min | Backlog |
| 15 | Installer `radon` (dev-requirements) pour activer le check de complexité cyclomatique du rituel `/friday` | tous | P3 | 15 min | Backlog |
| 16 | Épingler `image: :latest` sur un tag `sha-` immuable dans `infrastructure/kubernetes/backend.yaml` | K8s | P3 | 15 min | Backlog |
| 17 | Ne plus committer la ressource K8s `Secret` en clair (même vide) — renommer `.example` ou gérer hors repo | K8s | P3 | 20 min | Backlog |
| 18 | Ajouter un grep email/nom-propre aux checks sécurité du rituel `/friday` (angle mort identifié ce jour) | .claude | P3 | 10 min | Backlog |
| 19 | Clarifier avec Steven : contradiction règle #7 CLAUDE.md ("`.claude/` gitignoré en permanence") vs `.gitignore`/état réel (`.claude/rules|skills|agents` déjà publics) | .claude | P2 | 15 min (décision) | Semaine N+1 |

**Légende priorité** : P1 = bloquant ou sécurité · P2 = qualité importante · P3 = amélioration

## Addendum — reprise après réception des skills manquants (même jour, 2026-08-24)

L'utilisateur a fourni un second paquet (`docker_covid_friday_deps.zip`) contenant les 7 skills
et 4 commands manquants. Déplacés dans `.claude/skills/` et `.claude/commands/` (scannés avant
copie — RAS : pas de secret, pas d'unicode caché, pas de chemin local résiduel). Reprise des
phases initialement bloquées ci-dessous. `.claude/commands/` n'est pas exempté dans
`.gitignore` (contrairement à `.claude/skills/`) — utilisable en local, non versionné en l'état.

### repo-scan (Phase 2)

Outil taillé pour de gros monorepos C++/Android/iOS/Web avec dépendances vendorisées
(FFmpeg, Boost, OpenSSL...) — hors cible pour ce repo Python pur. Vérifié quand même :
aucun submodule, aucun `vendor/`/`third_party/`, aucun artefact de build committé,
6.2 Mo trackés au total. **RAS, mais outil à ne pas reproposer pour ce repo** (à faire
remonter à skill-stocktake).

### production-audit (Phase 2)

**Production audit : 61/100, risky.** Deux blockers qui capent le score à 69 (auth manquante
sur donnée sensible + chemin critique non testé end-to-end en CI), plus une série de
high-value fixes.

**Blockers :**
- **Auth fail-open par défaut** : `Settings.api_key` vide par défaut
  (`backend/app/config.py`), `verify_api_key()` laisse passer sans clé si `api_key` est vide
  (`backend/app/api/security.py`) — **aucun garde-fou fail-fast** si `API_ENV=production` et
  `API_KEY=""`. Le manifeste K8s `Secret` (`infrastructure/kubernetes/configmap.yaml`) livre
  justement `API_KEY: ""` par défaut avec un simple commentaire "Définir en prod" — un
  déploiement qui oublie cette étape expose `/api/v1/predict` sans authentification.
- **`uvicorn ... --reload` en CMD de production** (`infrastructure/docker/backend/Dockerfile`,
  dernière ligne) — flag dev-only (auto-reload sur changement de fichier) baké dans l'image
  buildée par la CI et utilisée par le smoke-test de déploiement et par
  `infrastructure/kubernetes/backend.yaml`.
- **Chemin critique non testé end-to-end en CI** : le smoke-test de `cicd.yml` (job `deploy`)
  vérifie `/health` et `/docs`, jamais un vrai appel à `/api/v1/predict` avec une image.

**High-value fixes :**
- `backend/app/api/predict.py:109` — le handler d'erreur générique renvoie `str(e)` brut au
  client dans le corps de la réponse 500 (fuite de détail interne, contredit
  `.claude/rules/common/security.md` "Error messages don't leak sensitive data").
- Aucun `USER` non-root dans `infrastructure/docker/backend/Dockerfile` ni
  `data-service/Dockerfile` — les deux tournent en root. Idem côté K8s : pas de
  `securityContext`/`runAsNonRoot` dans `infrastructure/kubernetes/backend.yaml`.
- `infrastructure/kubernetes/backend.yaml` déploie `image: ds-covid-backend:latest` — tag
  mutable, alors que la CI publie déjà des tags `sha-<court>` immuables. Pas de référence
  claire pour un rollback depuis le manifeste seul.
- CORS `allow_origins=["*"]` (déjà noté en section Sécurité) — à restreindre avant toute
  exposition publique réelle (Phase 3 K8s non déployée à ce jour).
- Commit d'une ressource Kubernetes `Secret` (même avec des valeurs vides) dans un repo
  public — pattern à risque si quelqu'un remplit des vraies valeurs et commit par
  inadvertance ; préférer un nommage `.example` + instructions de gestion externe des secrets.

**Evidence checked** : `backend/app/{main,config,api/predict,api/security}.py`,
`data-service/src/data_service/main.py`, `infrastructure/kubernetes/{configmap,ingress,
backend}.yaml`, les deux Dockerfiles, `.env.example`, `.github/workflows/cicd.yml`, tests
locaux (backend 57 tests verts, data-service 24 tests verts).

**Evidence missing** : statut réel du dernier run CI sur GitHub (non vérifié via `gh`),
confirmation que les manifestes K8s ont déjà été appliqués quelque part, durcissement de
MLflow/Postgres/MinIO (hors périmètre de cette passe).

**Next action** : les deux blockers sont des corrections rapides (retirer `--reload`, ajouter
un fail-fast sur `API_KEY` vide en prod) — voir tableau des actions priorisées, mis à jour.

### security-scan (Phase 3)

`security-scan` s'appuie par défaut sur `npx ecc-agentshield` (paquet npm tiers non installé,
non pinné) — or `production-audit` liste explicitement "lancer un `npx <package>@latest` non
pinné" comme anti-pattern. Choix : équivalent manuel plutôt que d'exécuter du code tiers non
revu. Couverture manuelle : `CLAUDE.md`, `.mcp.json` (absent), `.claude/hooks/` (absent),
`.claude/settings.local.json`, `.claude/agents/*.md` (tools scopés raisonnablement).

**Finding critique** : `.claude/settings.local.json` contient des entrées wildcard dans
`permissions.allow` — `Bash(git push *)`, `Bash(git merge *)`, `Bash(git commit *)`,
`Bash(git checkout *)`, `Bash(rm -rf *)`, `Bash(python -c :*)`, `Bash(node -e :*)` — qui
court-circuitent silencieusement la section "Validation humaine obligatoire" de `CLAUDE.md`
(laquelle exige explicitement un arrêt avant `git push`/`git merge`/suppression de
fichiers). Fichier non tracké par git (pas de fuite publique), mais il défait activement le
filet de sécurité documenté, sur cette machine, dès maintenant. `python -c :*` et
`node -e :*` équivalent en pratique à une exécution de code arbitraire pré-approuvée.
**P1 — à nettoyer dans `.claude/settings.local.json` : retirer ou resserrer ces wildcards.**

Découverte annexe (pas un finding de cette session, mais pertinent) : `docs/CLEANUP.md`
documente un audit antérieur (commit `aaf4d18`, déjà en place avant le début de ce rituel)
qui a trouvé et corrigé une vraie fuite — nom d'un projet client tiers réel, email personnel
réel (`rafael.cepa@cnrs-orleans.fr`), chemin local `/home/cepa/DST/...` dans `test.ipynb`.
Vérifié sur disque : correctement scrubbé, rien de résiduel. **Angle mort méthodologique
noté pour moi-même** : mes greps sécurité de ce rituel ciblaient `password|api_key|token|
secret`, jamais de pattern email/nom propre — à ajouter aux prochains `/friday`.

### refactor-clean (Phase 4)

Le Step 3 du skill ("Safe Deletion Loop") supprime du code sans confirmation par item — en
conflit avec `CLAUDE.md` "Validation humaine obligatoire" (suppression de fichiers) et la
philosophie même de `/friday`. Arrêté après Steps 1-2 (détection/catégorisation), rien
supprimé. `vulture` non disponible sur cette machine pour une détection plus poussée.

Résultat : rien de nouveau au-delà des 6 imports inutilisés déjà repérés par `ruff F401`
(dont 2 dans les tests du skill `skill-comply` lui-même, hors périmètre projet). Vérifié que
`prepro.py` (visible dans l'historique de la semaine) n'est plus présent — fusionné dans
`scripts/preprocess.py` comme documenté par les messages de commit, pas un résidu orphelin.

### update-codemaps + update-docs (Phase 5)

Codemaps générés dans `docs/CODEMAPS/` (`architecture.md`, `backend.md`, `frontend.md`,
`data.md`, `dependencies.md`) — première génération, pas de diff possible. Rapport dans
`.reports/codemap-diff.txt`.

`README.md` : deux sections corrigées pour refléter les changements de la semaine (stage
`augment` ajouté au pipeline DVC, absent du schéma ; `epochs: 10` obsolète vs `epochs: 30`
réel, params CLAHE/masking/cropping absents de l'exemple). Pas de `CONTRIBUTING.md`/
`RUNBOOK.md` créés — hors du périmètre annoncé ("mettre à jour les READMEs"), à trancher
séparément si souhaité.

### skill-comply (Phase 7)

**Non exécutable sur cette machine** : dépendance incompressible au binaire CLI `claude -p`
(vérifié dans `scripts/spec_generator.py` et `scripts/scenario_generator.py` — même le mode
`--dry-run` shell out vers `claude -p`), absent ici, et `uv` absent également. Conformément à
l'instruction explicite du skill lui-même ("ne pas produire de rapport partiel ou fabriqué"),
aucune tentative de contournement, aucun rapport généré.

### eval-harness (Phase 7)

Pas un outil de scan mais un framework méthodologique ("définir les évals AVANT de coder").
Aucun `.claude/evals/` existant, aucune feature en cours d'implémentation dans cette session
à laquelle rattacher une définition d'éval — rien à évaluer, cohérent avec la clause de
`/friday` elle-même ("sinon noter 'rien à évaluer ce mois-ci'"). Pas de définition fabriquée
pour une feature inexistante.

### skill-stocktake (Phase 7)

Full Stocktake + périmètre étendu (agents/rules), 37 items évalués : 18 skills, 4 commands,
7 agents, 8 rules. `scan.sh`/`quick-diff.sh`/`save-results.sh` dépendent tous de `jq`, absent
de cette machine — contournement par inventaire manuel (Read/Glob), confirmé viable. Verdicts
complets dans `~/.claude/skills/skill-stocktake/results.json` (chemin global) et détail des
verdicts non-évidents dans `.claude/ECC-TOOLING-AUDIT.md` (nouveau fichier).

**Retire (2) — proposés, rien supprimé sans confirmation :**
- `repo-scan` (skill) : conçu pour du C++/Android/iOS/Web avec libs vendorisées — mismatch
  confirmé avec ce repo Python pur (déjà noté plus haut). Impact : `friday` Phase 2 perd son
  seul outil pour les semaines sans infra — à corriger dans `SKILL.md` si retiré.
- `/prune` (command) : shell-out vers `continuous-learning-v2`, qui n'existe nulle part sur
  cette machine (vérifié global + projet) — cassé, sans rapport avec le nettoyage de code
  mort malgré ce que `friday` Phase 4 laisse penser ("`/refactor-clean` ou `/prune`").

**Improve (13)** — détail complet dans `results.json` ; les plus actionnables :
- `friday` : Phase 4 référence `/prune` (voir Retire) et un agent `comment-analyzer`
  inexistant — cassé à chaque exécution.
- `security-reviewer` (agent) : section "Analysis Commands" invoque `npm audit`/`npx eslint`
  — sans objet sur ce projet 100% Python, devrait pointer vers `pip-audit`/`bandit`.
- `mle-workflow`, `mle-reviewer` : référencent ~9-25 skills/agents inexistants dans ce projet.
- `deployment-patterns` : son exemple CI/CD générique contredit
  `github-actions-security.md` (pas de `permissions:` par job, tags mutables).
- `security-scan`, `skill-comply`, `rules-distill`, `skill-stocktake` (lui-même) : dépendances
  CLI manquantes (`npx`, `claude`, `jq`) sans guard de présence documenté.
- `python-patterns` : duplique le tableau des limites de taille de fichier déjà défini dans
  `coding-style.md` — risque de dérive entre les deux copies.
- `testing.md`, `code-review.md` : voir finding transversal ci-dessous.

**Keep (22)** : tout le reste, dont `monday` (vérifié indépendamment pour le même pattern de
références fantômes que `friday` avait — aucun trouvé), `production-audit`/`update-codemaps`/
`update-docs` (confirmés fonctionnels cette session), `import_cascade.md`/`sonarqube.md`/
`github-actions-security.md`/`coding-style.md` (règles fortes, dérivées d'audits réels).

**Finding transversal (le plus important)** : `testing.md` ("Minimum Test Coverage: 80%"),
`code-review.md` ("Test coverage meets 80% minimum") et l'agent `tdd-guide.md` ("Ensures
80%+ test coverage") affirment tous 80% en dur — **contredit** `CLAUDE.md` qui fixe le seuil
réel du projet à 40% backend / 30% data-service. `testing.md`/`code-review.md` sont
auto-chargés à chaque session et `tdd-guide` est un agent à invocation proactive : la
contradiction est vivante en permanence, pas juste théorique, et pourrait faire bloquer du
travail à tort à 11 jours de la soutenance (ex. `tdd-guide` bloquant à 45% alors que le seuil
réel est 40%).

### rules-distill (Phase 7)

Scripts `scan-skills.sh`/`scan-rules.sh` non exécutables (même dépendance `jq` manquante) —
Phase 1 faite manuellement à partir de l'inventaire skill-stocktake déjà réalisé. Un candidat
soumis à validation (jamais appliqué automatiquement, règle explicite du skill) :

**Candidat — Revise `rules/common/testing.md` §Minimum Test Coverage**
- **Preuve** : `testing.md`, `code-review.md`, `tdd-guide.md` (agent) tous à 80% en dur —
  voir finding transversal ci-dessus.
- **Risque si non corrigé** : contradiction vivante à chaque session, risque de blocage de
  travail légitime par un agent proactif se fiant au mauvais seuil.
- **Avant** : `## Minimum Test Coverage: 80%`
- **Après (proposé)** : `## Minimum Test Coverage\n\nSeuils définis par le projet dans
  CLAUDE.md (actuellement : backend ≥ 40%, data-service ≥ 30%). Ne jamais les abaisser (voir
  CLAUDE.md § Qualité). 80% reste la cible générique par défaut hors contexte projet
  spécifique.` — pointe vers CLAUDE.md comme source de vérité plutôt que de dupliquer les
  chiffres (même risque de dérive que `python-patterns` vs `coding-style.md`, déjà noté).
- **Confiance** : haute. **Portée** : la même correction s'impose dans `code-review.md` et
  `tdd-guide.md` par cohérence, mais c'est hors du périmètre strict "fichiers de règles" de
  `rules-distill` — à traiter comme un correctif manuel groupé si approuvé.

## Actions appliquées cette session (post-validation utilisateur)

Toutes les décisions ci-dessus ont été soumises à validation explicite avant application —
rien n'a été modifié en silence. Décisions et exécution :

- ✅ **P1 nettoyés** : wildcards dangereux retirés de `.claude/settings.local.json`
  (`git push/merge/commit/checkout *`, `rm -rf *`, `python -c :*`, `node -e :*` — JSON
  re-validé) ; `--reload` retiré du `CMD` de `infrastructure/docker/backend/Dockerfile`.
- ✅ **Seuil de couverture unifié à 80%** (décision utilisateur : aligner CLAUDE.md sur
  `testing.md`/`code-review.md`/`tdd-guide.md` plutôt que l'inverse). Appliqué dans
  `CLAUDE.md` (2 endroits), `Makefile` (`test-be`/`test-ds`), `.github/workflows/cicd.yml`
  (`test_be`/`test_ds`), `infrastructure/scripts/check_quality.sh`, `README.md`. **Effet
  immédiat vérifié** : `data-service` passe (83.3%), **`backend` échoue désormais** (77.54%
  mesuré < 80%) — accepté explicitement par l'utilisateur, à résorber en écrivant plus de
  tests backend (voir action prioritaire ci-dessous). `docs/us-verification.md`,
  `docs/ARCHITECTURE_MICROSERVICES.md` et `verify.sh` (check US-04) **non touchés** —
  ce sont des registres d'acceptation historiques d'une story déjà cochée à l'ancien seuil
  40%, pas des portes de qualité vivantes ; à trancher séparément si vous voulez que
  `verify.sh` reflète aussi le nouveau seuil.
- ✅ **Retraits confirmés** : `.claude/skills/repo-scan/` et `.claude/commands/prune.md`
  supprimés. `friday/SKILL.md` corrigé en conséquence (Phase 2 : table repo-scan retirée ;
  Phase 4 : `/prune` retiré de l'instruction, check `comment-analyzer` désactivé avec note ;
  "Voir aussi" nettoyé).
- ✅ **15 corrections "Improve" appliquées** (décision : tout maintenant) : `deployment-patterns`
  (note github-actions-security.md prioritaire), `mle-workflow` (2 tables élaguées aux
  skills/agents réels), `python-patterns` (table de tailles remplacée par un pointeur vers
  `coding-style.md`), `rules-distill`/`skill-stocktake` (guard `jq` + fallback manuel
  documenté), `security-review` (note stack TS + section Solana retirée), `security-scan`
  (`ecc-agentshield` pinné en `1.4.0`, vérifié sur npm, + fallback sans npm documenté),
  `skill-comply` (hedge `--dry-run` converti en fait vérifié : dépend aussi de `claude -p`),
  `refactor-clean` (garde-fou de confirmation ajouté avant Step 3), `mle-reviewer` (liste
  élaguée à 3 agents réels), `security-reviewer` (npm audit/eslint → pip-audit/bandit,
  vérifié cohérent avec `python-reviewer.md`), `tdd-guide` (exemples npm → pytest),
  `code-review.md` (table Agent Usage élaguée). `testing.md` : aucun changement nécessaire,
  son 80% est désormais correct.
- `.claude/ECC-TOOLING-AUDIT.md` créé (reconstruit après perte du fichier original écrit par
  l'agent dans un worktree nettoyé entre-temps — contenu récupéré intégralement depuis
  `results.json`, qui a bien survécu).

## Bilan

Grosse semaine de nettoyage (code mort supprimé en masse) et de fiabilisation du pipeline DVC
(fuite train/test corrigée, OOM corrigé, stage augment ajouté) — bon signal de fond côté code.
Le rituel `/friday` complet (avec Phase 7 exceptionnellement lancée dès le premier passage) a
surtout mis en lumière la dette du **dispositif Claude Code lui-même** plutôt que celle du
code applicatif, et cette dette a été traitée dans la foulée après validation : 2 P1 nettoyés
(permissions wildcard, `--reload` en prod), seuil de couverture unifié à 80% (backend en échec
assumé jusqu'à plus de tests), 2 retraits (`repo-scan`, `/prune`) et 15 corrections de contenu
appliquées sur les 37 fichiers `.claude/`. Reste : le score production-audit à 61/100 (auth
fail-open, chemin critique non testé en CI) n'a pas encore été corrigé dans le code applicatif
lui-même — voir actions #3-5 du tableau, à planifier la semaine prochaine.
