# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
backend/.venv/
data-service/.venv/
*.egg-info/

# Logs locaux (contenu ignoré, dossiers trackés via .gitkeep)
tmp/logs/
tmp/quality/
!tmp/logs/.gitkeep
!tmp/quality/.gitkeep
*.pid

# Env
.env
.venv
data/
__pycache__/
*.pyc

# Secrets K8s réels (le gabarit secrets.yaml.example reste tracké)
kubernetes/secrets.yaml

# Data & modèles (versionnés via DVC en Phase 2)
data/raw/
data/processed/
data/models/*.keras
data/models/*.h5
data/models/*.pkl

# DVC
.dvc/cache/
.dvc/tmp/
.dvc/config.local

# MLflow (tracking géré par le serveur Docker Postgres+MinIO, pas par git)
mlruns/
mlartifacts/
mlflow.db
.trash/

# Notebooks
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# Coverage
.coverage
htmlcov/
coverage.xml
*-coverage.xml

# Zip temporaires
_zip_extracted/
*.zip

# Dossiers générés localement (racine uniquement)
/models/
# Les artefacts de sortie sont régénérés localement et ne sont jamais versionnés.
/outputs/*
/tmp/

# Claude Code (mémoire IA + config locale non partagée)
.claude/*
!.claude/rules/
!.claude/skills/
!.claude/agents/
!.claude/CLAUDE_AUDIT.md

# Fichiers de travail locaux
plan_de_base.txt
*.docx
infrastructure/kubernetes/secrets.yaml

# Bilan perso (git log/PR par personne) — usage strictement personnel, jamais partagé
bilan_contribution_docker_covid.html
bilan_contribution_ds_covid.html

# Notes de présentation perso — usage strictement personnel, jamais partagé
notes_soutenance_steven.html
